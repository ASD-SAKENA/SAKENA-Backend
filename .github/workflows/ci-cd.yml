name: CI/CD Backend

# Fail fast → build/push GHCR → scan → k8s deploy (namespace `sakena`) → verify.
#
# `ci` also runs on pull requests so a broken build/test never reaches `main`
# in the first place; build/scan/deploy only run on pushes to `main` (or
# tags), same gating as before.

on:
  push:
    branches: [main]
    tags: ["v*.*.*"]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ghcr.io/asd-sakena/sakena-backend
  NAMESPACE: sakena
  DEPLOYMENT: sakena-backend
  CONTAINER: sakena-backend

concurrency:
  group: backend-cicd-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  ci:
    name: Test
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Run tests
        run: ./gradlew test --no-daemon --stacktrace

      - name: Upload test report
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: backend-test-report
          path: build/reports/tests/test/
          if-no-files-found: ignore

  build-and-push:
    name: Build, scan & push
    needs: [ci]
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      contents: read
      packages: write
    outputs:
      image_tag: ${{ steps.vars.outputs.sha_short }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Short sha
        id: vars
        run: echo "sha_short=${GITHUB_SHA:0:7}" >> "$GITHUB_OUTPUT"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract image metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,format=short
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # Scans the tag we're about to deploy. A CRITICAL finding fails the
      # job here, before the deploy job (which needs this one) ever runs -
      # the image stays published under its sha tag for forensics, but
      # nothing rolls it out.
      - name: Scan image for critical vulnerabilities
        uses: aquasecurity/trivy-action@0.28.0
        with:
          image-ref: ${{ env.IMAGE_NAME }}:sha-${{ steps.vars.outputs.sha_short }}
          format: table
          severity: CRITICAL
          exit-code: "1"
          ignore-unfixed: true

  deploy:
    name: Deploy
    needs: [build-and-push]
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      IMAGE_TAG: sha-${{ needs.build-and-push.outputs.image_tag }}
    steps:
      - name: Set up kubectl
        uses: azure/setup-kubectl@v4

      - name: Write kubeconfig
        run: |
          set -euo pipefail
          install -m 600 /dev/null "$RUNNER_TEMP/kubeconfig"
          echo "${{ secrets.KUBE_CONFIG_SAKENA }}" | base64 -d > "$RUNNER_TEMP/kubeconfig"
          echo "KUBECONFIG=$RUNNER_TEMP/kubeconfig" >> "$GITHUB_ENV"

      - name: Roll out new image
        run: |
          set -euo pipefail
          kubectl -n "$NAMESPACE" set image "deployment/$DEPLOYMENT" \
            "$CONTAINER=$IMAGE_NAME:$IMAGE_TAG" --record=false

      - name: Wait for rollout, roll back on failure
        run: |
          set -euo pipefail
          if ! kubectl -n "$NAMESPACE" rollout status "deployment/$DEPLOYMENT" --timeout=180s; then
            echo "::error::Rollout failed - rolling back deployment/$DEPLOYMENT"
            kubectl -n "$NAMESPACE" rollout undo "deployment/$DEPLOYMENT"
            kubectl -n "$NAMESPACE" rollout status "deployment/$DEPLOYMENT" --timeout=120s || true
            echo "--- describe ---"
            kubectl -n "$NAMESPACE" describe "deployment/$DEPLOYMENT"
            echo "--- recent pod logs ---"
            kubectl -n "$NAMESPACE" logs -l app="$DEPLOYMENT" --tail=100 --since=10m || true
            exit 1
          fi

      - name: Smoke test
        run: |
          set -euo pipefail
          for i in $(seq 1 15); do
            if curl -fsS https://api.sakena.aptcodegen.online/actuator/health | grep -q '"status":"UP"'; then
              echo "Backend is healthy"
              exit 0
            fi
            sleep 4
          done
          echo "::error::Smoke test failed - backend did not report healthy after rollout"
          exit 1
